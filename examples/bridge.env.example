# Copy to /etc/sutang-telegram-bridge/bridge.env and chmod 0600.
# Never commit the populated file.

AGENT_A_TELEGRAM_BOT_TOKEN=replace_me
AGENT_B_TELEGRAM_BOT_TOKEN=replace_me
AGENT_C_TELEGRAM_BOT_TOKEN=replace_me

# IDs and routing live in config.json; secrets stay here.
BRIDGE_LOG_LEVEL=INFO
